<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.jyf.UserDao">
          <select id="queryUser" resultType="map" parameterType="int">
              select m.menuname as menuname,m.menuurl as menuurl from
                    menu as m,
                    post_menu as pm,
                    usersurface as u,
                    user_post as up
                    where
                    u.UserId=up.UserId and up.PostId=pm.PostId and m.MenuId=pm.MenuId
                    and   u.UserId=#{id}
          </select>
</mapper>
